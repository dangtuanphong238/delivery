import DialogComponent from './DialogComponent';
import DialogManager from './DialogManager';
import { ScaleAnimation } from './popup-container';

export { DialogComponent, ScaleAnimation };

export default new DialogManager();
