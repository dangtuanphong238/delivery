import {} from '../actions/types';

const authState = {
  isLogedIn: false,
};

export const authReducer = (state = authState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
